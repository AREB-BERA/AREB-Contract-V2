[profile.default]
src = "src"
out = "out"
libs = ["lib"]

[rpc_endpoints]
areb = "${BERACHAIN_RPC_URL}"
verify_url = "${VERIFY_URL}"
verify_token = "${VERIFY_TOKEN}"

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
